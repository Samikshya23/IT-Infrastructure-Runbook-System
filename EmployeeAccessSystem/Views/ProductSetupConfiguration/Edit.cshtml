@model EmployeeAccessSystem.Models.ProductSetupConfiguration

@{
    ViewData["Title"] = "Edit Product Setup Configuration";
}

<div class="card card-primary">
    <div class="card-header">
        <h3 class="card-title">Edit Product Setup Configuration</h3>
    </div>

    <form asp-action="Edit" method="post">

        <div class="card-body">

            <input type="hidden" asp-for="NodeId" />
            <input type="hidden" asp-for="ProductId" />

            <div class="form-group">
                <label>Configuration Level</label>
                <input type="text"
                       class="form-control"
                       value="@Model.ConfigurationNodeName"
                       readonly />
            </div>

            <div class="form-group">
                <label>Value</label>

                @if (Model.InputType == "Date")
                {
                    <input asp-for="NodeValue"
                           type="date"
                           class="form-control"
                           required />
                }
                else
                {
                    <input asp-for="NodeValue"
                           type="text"
                           class="form-control"
                           required />
                }
            </div>

        </div>

        <div class="card-footer text-right">
            <a asp-action="Index"
               asp-route-productId="@Model.ProductId"
               class="btn btn-secondary">
                Back
            </a>

            <button type="submit" class="btn btn-primary">
                Update
            </button>
        </div>

    </form>
</div>