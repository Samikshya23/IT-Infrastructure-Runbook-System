@model EmployeeAccessSystem.Models.FormConfiguration

<div class="mb-2">

    <div class="border rounded bg-white px-2 py-2">

        <div class="row align-items-center">

            <div class="col-md-12">

                <span class="font-weight-bold">
                    @Model.NodeName
                </span>

                @if (!string.IsNullOrWhiteSpace(Model.InputType))
                {
                    <span class="badge badge-secondary ml-2">
                        @Model.InputType
                    </span>
                }

            </div>

        </div>

    </div>

    @if (Model.Children != null && Model.Children.Count > 0)
    {
        <div class="ml-4 mt-2 border-left pl-3">

            @for (int i = 0; i < Model.Children.Count; i++)
            {
                @await Html.PartialAsync("~/Views/FormConfiguration/_TreeNode.cshtml", Model.Children[i])
            }

        </div>
    }

</div>